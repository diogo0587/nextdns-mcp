interface:
  display_name: "Nextdns Mcp"
  short_description: "Repo-specific patterns and workflows for nextdns-mcp"
  default_prompt: "Use the nextdns-mcp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true